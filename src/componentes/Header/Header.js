import "./Header.css"

function Header() {
    return <header className="header">
        <img src="/img/header.png" alt="header de Org" />
    </header>
}

export default Header //Hay que exportar porque si no no deja importar

